<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.PhotosApp" parent="Base.Theme.PhotosApp">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@color/bgr_color</item>
        <item name="android:statusBarColor">@color/bgr_color</item>
        <item name="android:windowLightStatusBar">@color/bgr_color</item>
    </style>
</resources>